# ===== SafeCity - dépendances backend =====

# --- Backend web / temps réel ---
# IMPORTANT : Flask-SocketIO récent exige python-socketio >= 5.12 (Server.reason).
Flask>=3.0.3
Flask-Cors>=4.0.1
Flask-SocketIO>=5.4.0
python-socketio>=5.12.0
python-engineio>=4.11.0
SQLAlchemy>=2.0.31
Flask-SQLAlchemy>=3.1.1
Flask-Migrate>=4.0.0        # migrations de schéma (Alembic) — évolution sans perte
python-dotenv>=1.0.1

# --- Observabilité (optionnel) ---
# Suivi d'erreurs en production, actif si SAFECITY_SENTRY_DSN est défini.
sentry-sdk[flask]>=2.0.0

# --- Base de données professionnelle (PostgreSQL) ---
# Décommenter pour la production :
# psycopg2-binary>=2.9.9

# --- Sécurité ---
# ATTENTION : installer "PyJWT", PAS le paquet "jwt". En cas de conflit :
#   pip uninstall -y jwt PyJWT && pip install PyJWT
bcrypt>=4.2.0
PyJWT>=2.9.0
cryptography>=43.0.0

# --- Rapports PDF & export Excel ---
reportlab>=4.0.0
openpyxl>=3.1.0

# --- Intelligence artificielle (classification des incidents) ---
scikit-learn>=1.5.1
numpy>=1.26.4
# Optionnel (analyse d'image / avancé) :
# opencv-python>=4.10.0
# tensorflow>=2.17.0

# --- Déploiement ---
waitress>=3.0.0          # serveur WSGI (Windows / PyInstaller)
# eventlet>=0.36.1       # pour de vraies websockets (SAFECITY_ASYNC_MODE=eventlet)
# gunicorn>=22.0.0       # serveur WSGI (Linux/Docker)
# pyinstaller>=6.10.0    # exécutable Windows du poste opérateur

# --- Développement / tests ---
# pytest>=8.0.0
